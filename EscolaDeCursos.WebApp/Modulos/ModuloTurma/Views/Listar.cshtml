@{
    Layout = "_Layout";
    ViewBag.Titulo = "Listagem de Turmas";
}

@model List<ListarTurmaViewModel>;

<a class="btn btn-outline-primary rounded-pill px-4 mb-3" asp-action="Cadastrar">Cadastrar Nova</a>

@if (TempData["MensagemErro"] is string mensagemErro)
{
    <div class="alert alert-danger border-start border-danger border-4 rounded-3" role="alert">
        @mensagemErro
    </div>
}

@if (Model.Count == 0)
{
    <div class="alert alert-primary border-start border-primary border-4 rounded-3" role="alert">
        Nenhuma turma cadastrada.
    </div>
}
else
{
    <div class="row row-cols-1 row-cols-md-2 row-cols-xl-3 g-2">
        @foreach (ListarTurmaViewModel t in Model)
        {
            <div class="col">
                <div class="card h-100">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-start gap-3 mb-3">
                            <h5 class="card-title mb-0">@t.Nome</h5>
                            <span class="badge text-bg-primary rounded-pill">@t.QuantidadeMatriculas/@t.NumeroMaximoAlunos</span>
                        </div>

                        <dl class="row g-2 mb-4">
                            <dt class="col-5 text-secondary fw-semibold">Curso</dt>
                            <dd class="col-7 mb-0">@t.NomeCurso</dd>

                            <dt class="col-5 text-secondary fw-semibold">Instrutor</dt>
                            <dd class="col-7 mb-0">@t.NomeInstrutor</dd>

                            <dt class="col-5 text-secondary fw-semibold">Início</dt>
                            <dd class="col-7 mb-0">@t.DataInicio.ToString("dd/MM/yyyy")</dd>

                            <dt class="col-5 text-secondary fw-semibold">Término</dt>
                            <dd class="col-7 mb-0">@t.DataTermino.ToString("dd/MM/yyyy")</dd>
                        </dl>

                        <div class="d-flex flex-wrap gap-2 mt-auto justify-content-end">
                            <a class="btn btn-outline-primary btn-sm rounded-pill px-3" asp-action="Editar" asp-route-id="@t.Id">Editar</a>
                            <a class="btn btn-outline-primary btn-sm rounded-pill px-3" asp-action="Excluir" asp-route-id="@t.Id">Excluir</a>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
}
